import EventEmitter from "@scena/event-emitter";
import { DragScrollEvents, DragScrollOptions } from "./types";
export default class DragScroll extends EventEmitter<DragScrollEvents> {
    private startRect;
    private startPos;
    private prevTime;
    private timer;
    dragStart(e: any, options: DragScrollOptions): void;
    drag(e: any, options: DragScrollOptions): boolean;
    dragEnd(): void;
}
